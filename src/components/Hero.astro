---
---

<section class="hero">
  <h1>
    Ogden's <em>Basic</em> English
  </h1>
  <p class="subtitle">850 字繁中學習手冊 · 中英雙語 · 精煉版</p>
  <p class="meta">A controlled language designed by C.K. Ogden · 1930</p>
</section>

<style>
  .hero {
    background: linear-gradient(180deg, color-mix(in srgb, var(--bg-elev) 80%, var(--op-bg) 20%) 0%, var(--bg) 100%);
    border-bottom: 1px solid var(--line);
    padding: 80px 32px 56px;
    text-align: center;
  }
  h1 {
    font-family: var(--serif);
    font-weight: 600;
    font-size: clamp(36px, 6vw, 64px);
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 12px;
    line-height: 1.05;
  }
  h1 em {
    font-style: italic;
    color: var(--op);
  }
  .subtitle {
    font-family: var(--zh-serif);
    font-size: clamp(16px, 2.2vw, 20px);
    color: var(--ink-soft);
    margin-bottom: 8px;
  }
  .meta {
    font-family: var(--sans);
    font-size: 12px;
    color: var(--ink-faint);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-top: 16px;
  }
</style>
